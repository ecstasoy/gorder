apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: gorder-prom
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 5s
